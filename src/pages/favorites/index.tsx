import {Layout} from "@/layout";
import {FavoritesItems} from "@/components";

const FavoritesPage = (): JSX.Element => {

    return (
        <Layout>
            <FavoritesItems/>
        </Layout>
    )
}

export default FavoritesPage;